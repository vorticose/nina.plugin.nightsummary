<Window x:Class="NINA.Plugin.NightSummary.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Night Summary — Report Preview"
        Width="1100" Height="800"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#0d1117" Padding="10,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Data Source:" Foreground="#aaa" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="DataSourceCombo" MinWidth="280" SelectionChanged="DataSourceCombo_SelectionChanged" VerticalAlignment="Center"/>
                <Button Content="Update Preview" Click="UpdatePreview_Click" Width="180" Margin="12,0,0,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusText" Foreground="#888" FontStyle="Italic" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- WebView2 -->
        <wv2:WebView2 x:Name="PreviewWebView" Grid.Row="1"/>

        <!-- Loading overlay -->
        <Border x:Name="LoadingOverlay" Grid.Row="1" Background="#CC0d1117" Visibility="Collapsed">
            <TextBlock Text="Generating preview..." Foreground="#aaa" FontSize="16"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
